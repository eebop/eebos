[package]
name = "paging"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["dylib"]

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"


[dependencies]
dyshared = { version = "0.1.0", path = "../dyshared" }
static_assertions = { version = "1.1.0", features = ["nightly"] }
