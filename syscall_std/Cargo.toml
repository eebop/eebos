[package]
name = "syscall_std"
version = "0.1.0"
edition = "2024"

[dependencies]
zerocopy = { version = "0.8.27", default-features = false, features = ["alloc", "derive", "float-nightly", "simd", "zerocopy-derive"] }
